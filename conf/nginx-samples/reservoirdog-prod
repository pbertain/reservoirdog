# NGINX configuration for Reservoir Dog - Prod Environment
# Place this file in /etc/nginx/sites-available/
# Create symlink: ln -s /etc/nginx/sites-available/reservoirdog-prod /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name host74.nird.club;

    # Redirect HTTP to HTTPS (recommended for production)
    # return 301 https://$server_name$request_uri;

    # Or serve directly on HTTP
    location / {
        proxy_pass http://127.0.0.1:45080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Static files (if serving directly)
    location /static {
        alias /opt/prod/reservoirdog/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Images
    location /images {
        alias /opt/prod/reservoirdog/references/images;
        expires 7d;
    }

    # Logging
    access_log /var/log/nginx/reservoirdog-prod-access.log;
    error_log /var/log/nginx/reservoirdog-prod-error.log;
}

# HTTPS configuration (recommended for production - uncomment and configure)
# server {
#     listen 443 ssl http2;
#     server_name host74.nird.club;
# 
#     ssl_certificate /path/to/cert.pem;
#     ssl_certificate_key /path/to/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
# 
#     location / {
#         proxy_pass http://127.0.0.1:45080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

